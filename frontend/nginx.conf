server {
    listen 80;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # Support React Router: fallback to index.html if route not found
        try_files $uri /index.html;
    }
}
